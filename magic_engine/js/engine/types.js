export const MAGIC_LANES = Object.freeze({
  STABLE: "stable",
  WILD: "wild",
});

export const MAGIC_VISIBILITIES = Object.freeze({
  PUBLIC: "public",
  PRIVATE: "private",
});

export const MAGIC_OUTPUT_MODES = Object.freeze({
  TEXT: "text",
  JSON: "json",
  HYBRID: "hybrid",
});

export const MAGIC_ROUTING_CLASSES = Object.freeze({
  SPARK: "spark",
  SHAPE: "shape",
  VOICE: "voice",
});

export const MAGIC_PROVIDER_IDS = Object.freeze({
  OPENROUTER: "openrouter",
  LOCAL_FALLBACK: "local-fallback",
});

export const MAGIC_FALLBACK_KINDS = Object.freeze({
  POOL: "pool",
  TEMPLATE: "template",
  NONE: "none",
});
